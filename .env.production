# Stigmergy Configuration - Production
#
# This file contains configuration settings for the production environment.
# It is loaded when NODE_ENV is set to 'production'.
#
# Ensure all sensitive information, such as API keys and database credentials,
# are securely managed, preferably through environment variables injected
# during the deployment process (e.g., via Docker secrets or a secret manager).

# === CHOOSE YOUR AI PROVIDERS ===
# Reasoning Models (for planning, analysis, complex tasks)
REASONING_PROVIDER=google    # Options: 'google' or 'openrouter'
REASONING_MODEL=gemini-2.0-flash-thinking-exp  # or 'deepseek/deepseek-chat' for OpenRouter

# Strategic Models (for business planning, architecture)
STRATEGIC_PROVIDER=google    # Options: 'google' or 'openrouter'
STRATEGIC_MODEL=gemini-1.5-pro  # or 'anthropic/claude-3.5-sonnet' for OpenRouter

# Execution Models (for code implementation, documentation)
EXECUTION_PROVIDER=google    # Options: 'google' or 'openrouter'
EXECUTION_MODEL=gemini-1.5-flash  # or 'anthropic/claude-3.5-sonnet' for OpenRouter

# Utility Models (for simple tasks, validation)
UTILITY_PROVIDER=google      # Options: 'google' or 'openrouter'
UTILITY_MODEL=gemini-1.5-flash-8b  # or 'deepseek/deepseek-chat' for OpenRouter

# === AI PROVIDER API KEYS ===
# It is strongly recommended to manage these via environment variables
# in your deployment environment rather than hardcoding them here.
GOOGLE_API_KEY=${GOOGLE_API_KEY}
OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
FIRECRAWL_API_KEY=${FIRECRAWL_API_KEY}

# === SYSTEM OPTIMIZATION ===
# Production environments should be optimized for performance and stability.
ENABLE_COST_OPTIMIZATION=false
ENABLE_PERFORMANCE_MONITORING=true
MIN_SUCCESS_RATE=0.95
MAX_ERROR_RATE=0.05

# Disable development-specific features in production.
ENABLE_INTERNAL_DEV=false
ENABLE_GEMINI_CLI=false
ENABLE_QWEN_CLI=false

# === INFRASTRUCTURE ===
# The NEO4J_URI should point to your production database.
# When using Docker Compose, this will be the service name 'neo4j'.
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_secure_password # Replace with a secure password, or use environment variables.

# === SYSTEM CONFIGURATION ===
STIGMERGY_CORE_PATH=.stigmergy-core
NODE_ENV=production
