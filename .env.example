# .env.example

# --- Tiered AI Provider Configuration ---
# Provide keys for the models you intend to use. The system will select the appropriate
# tier based on the agent's configuration (s_tier, a_tier, b_tier).

# S-Tier (Strategic/Reasoning - Highest Capability) - e.g., for @saul, @metis
# Example: Claude 3 Opus via OpenRouter
OPENROUTER_API_KEY=

# A-Tier (Advanced/Execution - High Capability) - e.g., for @dev, @qa
# Example: GPT-4o via OpenAI
OPENAI_API_KEY=

# B-Tier (Basic/Utility - Fast & Cheap) - e.g., for simple, repetitive tasks
# Example: Llama3 8B via Groq
GROQ_API_KEY=

# --- Research Tool Configuration ---
# Firecrawl is used for deep web research by agents like @mary and @sally
FIRECRAWL_KEY=

# --- Neo4j Database Configuration (for Code Intelligence) ---
# This is optional if you run in 'memory' or 'auto' (fallback) mode.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# --- Engine Configuration ---
PORT=3000
