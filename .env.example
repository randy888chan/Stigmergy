# --- AI Provider Configuration ---
# The system uses a generic configuration for all AI providers.
# Set the following variables to connect to your preferred provider.

# The API key for your AI provider.
AI_API_KEY=

# The base URL for the API. This is only needed if you are not using OpenAI's default URL.
# Example for OpenRouter: https://openrouter.ai/api/v1
# Example for a local LLM: http://localhost:11434/v1
AI_API_BASE_URL=

# The model you want to use.
# Example for OpenRouter: "google/gemini-pro-1.5"
# Example for OpenAI: "gpt-4o"
# Example for a local LLM: "llama3"
AI_MODEL=

# --- Research Tool Configuration ---
# Firecrawl is now the standard tool for all research (search and scraping).
FIRECRAWL_KEY=

# --- Neo4j Database Configuration (for Code Intelligence) ---
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=stigmergy123

# --- Engine Configuration ---
PORT=3000

# --- External & Hybrid Agent Configuration ---

# GOOGLE_API_KEY is for direct API calls to Google AI, NOT for the Gemini CLI.
# The @gemini-executor agent will trigger a one-time browser login flow
# to authenticate the CLI with your Google account automatically.
GOOGLE_API_KEY=your_gemini_api_key_if_needed_for_direct_api_calls

# SUPERDESIGN_API_KEY is not currently used.
# The system connects to the SuperDesign VS Code extension as a local service,
# which does not require an API key.
# SUPERDESIGN_API_KEY=your_superdesign_key

# Determines the default agent selection strategy.
# Options: hybrid | external | internal
PREFERRED_MODE=hybrid

# The confidence threshold required for an agent to be selected.
TRUST_THRESHOLD=0.8