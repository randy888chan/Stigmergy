# Copy this file to .env and fill in your credentials.

# --- AI Provider Configuration ---
# The system will prioritize providers in this order: Fireworks.ai, then OpenAI.
# You only need to provide a key for the service you want to use.
FIREWORKS_KEY=
OPENAI_KEY=

# --- To use a custom OpenAI-compatible endpoint (like a local LLM server) ---
# OPENAI_ENDPOINT="http://localhost:11434/v1"
# CUSTOM_MODEL="llama3"

# --- Research Tool Configuration ---
# Firecrawl is used for deep research (searching and scraping). Get a free key.
FIRECRAWL_KEY=

# --- Neo4j Database Configuration (for Code Intelligence) ---
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_database_password

# --- Web Search API Configuration (for simple searches) ---
# Set your preferred search provider ("serper" or "brave")
SEARCH_PROVIDER=serper
SEARCH_API_KEY=your_serper_api_key_here
BRAVE_API_KEY=your_brave_api_key_here # (if using brave)

# --- Engine Configuration ---
# The port the local engine server will run on.
PORT=3000
