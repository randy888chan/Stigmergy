# Use Case: Full, end-to-end autonomous development.
# Environment: Local IDE
# Output: Production-ready code and tests.
lead_agent: "@specifier"
bundle:
  name: "Autonomous Core"
  icon: "⚙️"
  description: "The complete agent swarm for full, end-to-end project execution. Includes planning, execution, QA, and self-improvement."
  agents:
    # --- Core Workflow ---
    - "system"
    - "specifier"
    - "dispatcher"
    - "executor" # UPDATED: from dev/enhanced-dev
    - "qa"
    - "debugger"
    
    # --- Self-Improvement & Health ---
    - "metis"
    - "guardian"
