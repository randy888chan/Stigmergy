bundle:
  name: "Autonomous Core"
  icon: "⚙️"
  description: "The complete agent swarm for full, end-to-end project execution within a local IDE environment. Includes planners, executors, QA, and self-improvement."
  agents:
    # --- Orchestration & Planning ---
    - "dispatcher"
    - "specifier"
    - "reference-architect"
    
    # --- Core Execution ---
    - "executor"           # High-speed code implementation
    - "qa"                 # Runs tests and verifies quality
    - "debugger"           # Fixes bugs found by QA
    
    # --- Self-Improvement & Health ---
    - "health_monitor"     # Proactively monitors for issues
    - "metis"              # Learns from failures
    - "guardian"           # Securely applies improvements
```
**Use Case:** Full, end-to-end autonomous development.
**Environment:** **Local IDE**
**Output:** Production-ready code and tests.
