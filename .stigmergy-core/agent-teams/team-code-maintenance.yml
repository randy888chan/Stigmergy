# Use Case: Fixing a bug, refactoring a complex module, or improving test coverage in an existing project.
# Environment: Local IDE
# Output: Improved, tested, and higher-quality code.
bundle:
  name: "Code Maintenance"
  icon: "🔧"
  description: "A specialized team for maintaining and improving existing codebases. Focuses on debugging, refactoring, and quality assurance."
  agents:
    # === Core Interface & Orchestration ===
    - "system"              # KEPT: The main user interface
    - "dispatcher"          # KEPT: To manage the maintenance plan (if needed)

    # === Core Maintenance Crew ===
    - "debugger"            # KEPT: The primary bug fixer
    - "refactorer"          # KEPT: The code quality specialist
    - "qa"                  # KEPT: To verify the fixes and refactors

    # === Self-Improvement Loop ===
    - "metis"               # KEPT: To learn from the maintenance tasks
    - "guardian"            # KEPT: To safely apply learned improvements
