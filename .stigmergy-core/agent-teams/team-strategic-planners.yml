bundle:
  name: "Strategic Planners"
  icon: "📜"
  description: "A specialized team of planner agents designed for the Web UI. They create all necessary project documentation and plans, but do not execute code or run shell commands."
  agents:
    # --- Orchestration ---
    - "dispatcher"         # The orchestrator

    # --- Research & Analysis ---
    - "analyst"            # Performs market and user research
    - "reference-architect"# Finds proven patterns from external sources

    # --- High-Level Planning ---
    - "business_planner"   # Creates the business plan
    - "ux-expert"          # Creates the UX proposal
    - "design-architect"   # Creates the technical blueprint
    
    # --- Detailed Specification ---
    - "specifier"          # Creates the formal spec and machine-readable task breakdown
    
    # --- Validation ---
    - "valuator"           # Assesses business value
```
**Use Case:** Starting a new project from a high-level idea in the Web UI.
**Environment:** **Web UI**
**Output:** A complete set of project documents (`business-plan.md`, `architecture_blueprint.yml`, `spec.md`, `plan.md`, etc.).
